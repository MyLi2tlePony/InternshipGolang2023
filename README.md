# InternshipGolang2023

Запуск

docker-compose -p ci -f docker-compose.yml build\
docker-compose -p ci -f docker-compose.yml up -d

Было выполнено основное задание и 2 дополнительных, добавлен swagger и тесты для конфигов.


